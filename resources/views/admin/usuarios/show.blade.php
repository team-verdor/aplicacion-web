@extends('layouts.app')

@section('content')
    <div class="container">
        <div class="row">
            <div class="col-md-8 col-md-offset-2">
                <div class="panel panel-default">
                    <div class="panel-heading">Usuario {{ $usuario->id }}</div>
                    <div class="panel-body">

                        <a href="{{ url('administrar/usuarios/' . $usuario->id . '/edit') }}" class="btn btn-primary btn-xs" title="Edit Usuario"><span class="glyphicon glyphicon-pencil" aria-hidden="true"/></a>
                        {!! Form::open([
                            'method'=>'DELETE',
                            'url' => ['administrar/usuarios', $usuario->id],
                            'style' => 'display:inline'
                        ]) !!}
                            {!! Form::button('<span class="glyphicon glyphicon-trash" aria-hidden="true"/>', array(
                                    'type' => 'submit',
                                    'class' => 'btn btn-danger btn-xs',
                                    'title' => 'Delete Usuario',
                                    'onclick'=>'return confirm("Confirm delete?")'
                            ))!!}
                        {!! Form::close() !!}
                        <br/>
                        <br/>

                        <div class="table-responsive">
                            <table class="table table-borderless">
                                <tbody>
                                    <tr>
                                        <th>ID</th><td>{{ $usuario->id }}</td>
                                    </tr>
                                    <tr><th> Email </th><td> {{ $usuario->email }} </td></tr><tr><th> Nombreusuario </th><td> {{ $usuario->nombreusuario }} </td></tr><tr><th> Contrasena </th><td> {{ $usuario->contrasena }} </td></tr>
                                </tbody>
                            </table>
                        </div>

                    </div>
                </div>
            </div>
        </div>
    </div>
@endsection